`Basic syntax of the substring string.substring(start , end)

Rules are same as of slice only two rules are different 


in substring negative treated as 0 where in slice eit counts from the end

if start > end than it will swap the postion than it will do susbtring 



`

let stres = "Hello World";

let newStress = stres.substring(-5, 3);
console.log(newStress);